{
  "name": "particle-portfolio",
  "version": "0.1.0",
  "description": "GPGPU particle field that spells and morphs between phrases.",
  "scripts": {
    "dev": "npm run cases && vite",
    "build": "npm run cases && vite build",
    "preview": "vite preview",
    "cases": "node scripts/build-cases.mjs"
  },
  "type": "module",
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "private": true
}
